『消しプリ』のアプリ仕様書の初案を作成しました。
まずは仕様書としての全体像の定義、次に仕様上の不足点・曖昧な点の質問、最後にAIモデル検証方針と売れるアプリにするための提案をまとめています。

『消しプリ』アプリ基本仕様書 (Draft v1.0)
1. アプリ概要
* アプリ名: 消しプリ（ケシプリ）
* コンセプト: 「パシャッと撮って、1タップで新品。」
* ターゲット: 中学受験生や小中学生の保護者（プリントの解き直し作業の負担を減らしたい層）
* コア体験: 撮影から数タップで手書き文字・赤ペンが消えたクリーンなPDF/画像を出力する。
2. 画面構成・画面遷移図
[画面1: ホーム]
   ├── (カメラ起動) ────> [画面2-1: 撮影] ──(撮影完了)──┐
   ├── (アルバム選択) ─────────────────────────┼─> [画面3: 消去・保存プレビュー]
   └── (設定) ─────────> [画面4: 設定]         │     ├── [画面3-1: トリミング]
                                              │     └── [画面3-2: 消しゴム]
                                              └─ ※裏で自動補正・AI消去を実行
3. 各画面詳細仕様
【画面1：ホーム】
* 画面要素:
    * 上部: アプリタイトルロゴ（消しプリ）
    * 中央: 巨大な「カメラ」アイコンボタン（タップで画面2-1へ）
    * 下部: 「アルバムから選択」ボタン（PhotosPicker起動）
    * 最下部右/左: 「設定」アイコンボタン（画面4へ）
* 動作仕様:
    * 起動時のフットワークを最優先し、余計なダイアログを出さず即座にアクションへ導く。
【画面2-1：撮影画面】
* 画面要素:
    * 中央: カメラビューファインダー
    * 右上: フラッシュ切り替えボタン（Auto / On / Off）
    * 下部中央: 撮影シャッターボタン
    * 画面上（オーバーレイ）: 認識されたプリントの4頂点（白丸）と枠線（白ライン）
* 動作仕様:
    * カメラフレーム内で書類（紙）の四角形をリアルタイム検知し、枠線をオーバーレイ表示。
【画面3：消去・保存プレビュー】
* 画面要素:
    * メインエリア: AI処理完了後のプリント画像（ピンチイン/アウトで拡大縮小・パンスクロール可能）
    * 画面下部2段目（編集ツールバー）:
        * トリミング: 画面3-1をモーダル表示
        * 回転: 90度右回転
        * 消しゴム: 画面3-2をモーダル表示
        * 削除: 確認アラート表示後、ホーム（画面1）へ戻る
    * 画面下部1段目（アクションツールバー）:
        * 保存: カメラロール保存、またはPDFとしてファイル保存（iOS標準シェアシート）
        * 印刷: AirPrintダイアログ呼び出し
* 自動処理フロー:
    1. 撮影/選択画像をVisionフレームワークで台形補正（長方形化）
    2. モノクロ2値化・背景補正（完全な白背景化）
    3. AIモデルによる筆記（鉛筆線）および赤ペン（採点）の除去処理
【画面3-1：トリミング画面】
* 画面要素:
    * メインビュー: 検出されたプリント画像と4頂点ピン（丸アイコン）、エッジガイドライン
    * 下部1段目: キャンセル / 完了 ボタン
* 動作仕様:
    * 頂点ピンをドラッグ時、指で隠れないようピンの近傍（上部など）に拡大ルーペ（Magnifier）を表示。
    * 完了 タップ時、指定された4頂点をもとに透視変換（Perspective Transform）を行い長方形画像へ補正。
【画面3-2：消しゴム画面】
* 画面要素:
    * メインビュー: 処理中プリント画像（ピンチイン/アウト・移動対応）
    * 下部2段目: 消しゴムサイズ指定スライダー（操作中に画面上に現在の消しゴム直径プレビュー円を表示）
    * 下部1段目: キャンセル / アンドゥ (戻る) / リドゥ (進む) / 完了
* 動作仕様:
    * タッチドラッグした軌跡（ストローク）を純白（#FFFFFF）で塗りつぶす。
    * 履歴スタックを保持し、Undo/Redoの状態に応じて各ボタンのisEnabledを制御。
【画面4：設定画面】
* Pro版（課金領域）:
    * サブスクリプション状態表示（無料会員 / Pro会員、更新日・有効期限）
    * 「サブスクリプションを管理」（App Storeのサブスク管理画面を開くURL呼び出し）
    * Restore（購入の復元）ボタン
* 情報:
    * アプリバージョン表示
    * プライバシーポリシー（Web Viewまたは外部Safari）
    * お問い合わせ（mailto:リンク起動）
    * レビューする（SKStoreReviewController呼び出し）
    * アプリを共有する（UIActivityViewController呼び出し）
* デバッグ用（Buildモード限定）:
    * 無料枠カウントリセットボタン
    * AI推論時間計測ログ表示

4. 課金仕様 (マネタイズ)
* モデル: サブスクリプション（月額制）
* 価格案: 月額 200円〜280円 または 年額 1,980円（月換算165円） ※後述の提案参照
* 無料版制限: 1日1枚までの処理・保存（毎日午前0時にカウントリセット）。

仕様の確認・不足点・曖昧な点（質問事項）
実装・詳細仕様策定にあたり、以下の4点についてご確認・ご方針をお聞かせください。

1. 処理順序と手動トリミングの連携について
    * 現状の仕様では「撮影後、即座に自動補正＋AI処理を行って画面3を表示する」流れとなっています。
    * もし自動で四隅認識がズレていた場合、AI処理が終わった後にユーザーが「トリミング」を再調整することになります。この場合、「再トリミング後に再度AI消去処理を走らせる」設計で問題ないでしょうか？（または、撮影直後にまず自動トリミング確認画面を挟む方が処理速度的に無駄がないでしょうか？）⇨はい、そうです。自動トリミングの精度がよければ、一旦確認画面を挟む方が煩わしいです。
2. 2値化（白黒化）のタイミングと赤ペン除去
    * 赤ペン（赤丸）を検出・除去する際、カラー情報のRGB/HSV画像から処理するのが最も精度が高くなります。処理順序として「【カラー画像】赤丸・鉛筆除去 ➔ 【最後】にモノクロ2値化」というパイプラインでよろしいでしょうか？⇨ はい。
3. 「保存」のフォーマット
    * 「1タップで印刷・保存」の際、保存形式はPDF化メインでしょうか、それとも画像（JPEG/PNG）としてカメラロール保存がメインでしょうか？両方（シェアシートで選べる形）でしょうか？⇨両方です。
4. 無料枠の制限を超えた際の挙動
    * 1日1枚の制限に達した状態で2枚目を撮影しようとした場合、どのタイミング（撮影ボタン押下時 / プレビュー保存時）で課金プロンプト（Paywall画面）を表示させるかご希望はありますか？⇨プレビュー保存時で課金プロンプト表示させてください。

AIモデル検証方針（PoC計画）
「軽量・高速・オンデバイス処理」を実現するためのPoC（検証）ロードマップです。

1. 撮影時の領域認識（既存モジュールの選定）
* 結論: 新規にAIモデルを作る必要はなく、Apple標準のVision Frameworkにある VNDetectRectanglesRequest を使用します。
* 実績・特徴: iOS標準の書類スキャナー同等の精度で、四隅の自動検出、エッジ描画、台形補正（透視変換）まで完全オンデバイスかつリアルタイムで動作します。
2. 消去用AIモデルの選定とPoCステップ
消去処理は以下の2パターンで精度と速度を比較検証します。

* アプローチA：物体検出（YOLOv8n / YOLOv11n-seg）+ インペインティング（塗りつぶし）
    * YOLOv8-Nano（CoreMLに変換して数MB〜十数MB）を用いて「手書き文字」「赤丸・チェックマーク」のバウンディングボックス（またはセグメンテーション）を抽出。
    * 検出された領域を白で塗りつぶす（罫線部分などは簡易的な補間、または紙背景処理）。
    * メリット: 超高速（iPhone上で数十ミリ秒で動作）。
* アプローチB：画像変換ネットワーク（UNet / Pix2Pix系の軽量版）
    * 入力：手書きあり画像 ➔ 出力：手書きなし画像 を直接変換するモデル。
    * メリット: 印刷文字と罫線を綺麗に残しつつ文字だけ消せる。
    * 懸念: モデルサイズがやや大きくなりやすく、量子化（INT8/FP16化）が必要。

コメント：
＞まずはアプローチA（YOLOによる赤ペン・鉛筆ストローク検出 ➔ CoreML変換 ➔ iPhone実機での推論速度計測）から着手することを推奨します。
同意します。まずは、簡単に検証ができるようなpython環境を構築してください。トレーニング用のアノテーション用のツールも調査してください。
価格は「月額250円 / 年額1,980円（月約165円）」 でとりあえず行きます。

＞AVFoundationのカスタムカメラで「標準の撮影音（または静音処理）」に配慮された設計にする
同意します。


PoC最初の1歩:
まずはアプローチA（YOLOによる赤ペン・鉛筆ストローク検出 ➔ CoreML変換 ➔ iPhone実機での推論速度計測）から着手することを推奨します。

売れるアプリにするための提案
競合（『宿題スキャナー』等）が存在する中で、本アプリが勝ち切るための提案です。

1. 価格設定の提案：月額200〜280円 / 年額1,980円
* 理由: 月100円（年間1,200円）だと、Appleの手数料（30%）や返金・為替を考慮した際、LTV（顧客生涯価値）に対して広告費（CPI）を回収するのが難しくなります。
* 一方で、競合の「年4,500円（月375円相当）」はライトユーザーには心理的ハードルが高いです。
* 「月額250円 / 年額1,980円（月約165円）」 とすることで、「宿題スキャナーの半額以下」を強力に訴求でき、かつ収益性を確保できます。
2. 「印刷時のインク節約（完全白背景化）」の全面打ち出し
* 親御さん（特に中学受験層）が一番気にするのは「プリンターのインク代」と「紙の影（黒ずみ）」です。
* 影を完全に飛ばし、紙の地色を純白（#FFFFFF）に整える「インク節約モード/超クリア2値化」をアピールポイント（キャッチコピー）に入れると、ターゲット層に非常に刺さります。
3. 無音撮影機能
* 夜間や子供が勉強している横で連続して撮影・復元する際、シャッター音がストレスになります。AVFoundationのカスタムカメラで「標準の撮影音（または静音処理）」に配慮された設計にすると顧客満足度が上がります。
以上の仕様構成や質問内容について、フィードバックや修正のご希望があればお知らせください。
